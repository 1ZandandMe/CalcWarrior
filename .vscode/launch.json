{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Build & Run Current File",
            "type": "cppvsdbg",  // MSVC 调试器；如果用 MinGW 则改为 "cppdbg"
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",  // 依赖 CMake Tools 提供的路径
            "args": [],
            "stopAtEntry": false,
            "cwd": "${fileDirname}",
            "environment": [],
            "console": "integratedTerminal",
            "preLaunchTask": "CMake Build Current File Target"
        }
    ]
}