# CalcWarrior 项目进度

## 项目结构

```
CalcWarrior/
├── CPP/chatroom/           ← C++ WebSocket 聊天服务器
│   ├── server.cpp          ← 主服务（含 AI 命令处理）
│   ├── config.h            ← 配置（含 AI 服务地址）
│   ├── httplib.h           ← HTTP/WS 库（单头文件）
│   └── public/             ← 前端静态页面
│
├── pycode/                 ← Python 模块
│   ├── lanchat/            ← LAN Chat 核心库
│   │   ├── main.py         ← 聊天节点入口
│   │   ├── protocol.py     ← 消息编解码
│   │   ├── peer.py         ← 节点发现/心跳
│   │   ├── chat.py         ← 消息收发/ACK
│   │   ├── ui.py           ← 终端 UI（含 /ai 命令）
│   │   ├── config.py       ← LAN 配置
│   │   └── **ai.py**       ← [新] DeepSeek API 封装
│   │
│   ├── **ai_bot.py**       ← [新] 独立 AI 机器人（LAN 节点）
│   ├── **ai_service.py**   ← [新] AI HTTP 后端服务
│   └── 1.py
│
└── **写好了吗.md**          ← 本文件
```

## 架构图

```
┌─ 方案 A：LAN Chat 内 ──────────────────────────────┐
│                                                      │
│  python main.py ── UDP 广播 ──► 其他 LAN 节点        │
│       │                                              │
│       └── /ai 命令 ──► query_deepseek() ──► DeepSeek │
│                                                      │
├─ 方案 B：独立 AI 机器人 ─────────────────────────────┤
│                                                      │
│  python ai_bot.py ── UDP 广播 ──► LAN Chat 节点      │
│       │                   ◄──── 自动 AI 回复          │
│       └── query_deepseek() ──► DeepSeek API          │
│                                                      │
├─ 方案 C：C++ WebSocket 聊天室 ───────────────────────┤
│                                                      │
│  浏览器 ──WS──► C++ server ──HTTP──► ai_service.py   │
│                    :8080              :5005           │
│                                            │         │
│                                     query_deepseek() │
│                                            │         │
│                                         DeepSeek API │
└──────────────────────────────────────────────────────┘
```

## 已实现功能

### Python 层

| 组件 | 状态 | 说明 |
|------|------|------|
| `lanchat/ai.py` | ✅ | DeepSeek API 封装，零依赖（纯 urllib） |
| `lanchat/ui.py` | ✅ | 聊天内 `/ai` 命令，异步调 AI 不阻塞收发 |
| `ai_bot.py` | ✅ | 独立 AI 机器人，自动回复，每用户对话记忆 20 轮 |
| `ai_service.py` | ✅ | HTTP 服务，多线程，会话管理，上限 100 会话 |

### C++ 层

| 组件 | 状态 | 说明 |
|------|------|------|
| `config.h` | ✅ | AI_HOST / AI_PORT / AI_TIMEOUT_SEC 可配 |
| `server.cpp` | ✅ | `/ai` 命令拦截 → HTTP 调 Python → 广播回复 |
| 错误处理 | ✅ | AI 服务未启动时给用户友好提示 |

## 启动方式

```bash
# 0. 设置 API Key（三选一，用户自己填）
export DEEPSEEK_API_KEY=sk-xxx

# 1. AI HTTP 服务（方案 C 必需）
python pycode/ai_service.py

# 2. C++ 聊天服务器（方案 C）
cd CPP/chatroom
g++ -std=c++11 -O2 -static server.cpp -o server.exe -lws2_32 -lpthread
./server.exe
# 浏览器打开 http://localhost:8080

# 3. 独立 AI 机器人（方案 B）
python pycode/ai_bot.py

# 4. LAN Chat 终端（方案 A）
cd pycode/lanchat
python main.py
```

## 待办 / 可扩展

- [ ] AI 回复流式输出（SSE / WebSocket push）
- [ ] 前端 / 用户可配置 AI 系统提示词
- [ ] AI 服务健康检查 / 自动重启
- [ ] 通过 `conv_id` 支持多用户独立上下文

---

最后更新：2026-05-16
